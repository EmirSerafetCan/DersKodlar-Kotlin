fun main(Args: Array<String>) {

/*
    val sayi = 12
    val sonuc = if (sayi % 2 == 0) {
        "Sayı Çifttir"
    }
    else {
        "Sayı Tektir"
    }
    println(sonuc)

    val sayi1 = 7
    val sonuc1 = if (sayi1 % 2 == 1)"Sayı Tek" else "Sayı Çift"
    println(sonuc1)

    val liste = mutableListOf<Int>(1,2,3)   // ?=ELVİS OPERATÖRÜ -> ? solundaki durum geçerliyse
    val boyut = liste?.size?:-1             //değişkene sol kısmı atar, değilse sağ kısmı atar.
    println(boyut)

    var sayi = 2
    when(sayi) {
        2 ->
        {
            println("Sayı 2'dir")           //when = switch-case
        }                                   //if-else ile yapılan her şey when ile yapılabilir
        5 ->                                //when kullanımı if-else göre daha kolaydır.
        {
            println("Sayı 5'dir")
        }
        else ->
        {
            println("Sayı ne 2 ne 5'dir.")
        }
    }

    try {
        var sayi = 10/0
        println("İşlem Yapılıyor")
        println(sayi)
    }
    catch (e : ArithmeticException) {
        println("ArithmeticException Hatası")
    }

    finally {
        //Bu blok her zaman çalışır.
    }

    repeat(10)
    {
        println("index :" +it)    //repeat -> girilen değer kadar sayı döner. 0dan başlar. Sayılara it ile erişilir.
    }

    for (x in 1..10)
        println("x: " +x)

    for (y in 1..10 step 2)
        println("y: " +y)

    for (z in 10 downTo 1)
        println("z: " +z)              //down to -> geri geri döner

    for (k in 1 until 10)
        println("k: " +k)           //until sonuncu sayıyı dahil etmez

    for (a in "Medipol")
        println("a: " +a)

    val isimler = arrayOf("A", "B", "C")
    for (b in isimler)
        println("b: " +b)

    val isimler1 = arrayOf("A", "B", "C")
    for (c in isimler.withIndex())
        println("c: " +c)

    (1..10).forEach { x-> println("x: " +x) }

    val liste = mutableListOf("Can", "Efe", "Emir")
    liste.forEach{ liste -> println("isim: " +liste) }

    for (x in 1..10) {
        println("x: " + x)
        if (x == 3) {
            break
        }
    }
    */

}